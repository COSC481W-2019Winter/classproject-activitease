How to install project
1. Create new Android Studio project with Navigation drawer activity. MAKE SURE YOU NAME THE PROJECT 'AcitivitEase' AND NOTHING ELSE!
Other names will disorganize the package storage.
2. Put class files in class folder (Folder contains MainActivity on startup)
3. Put res files in their respective folders:
    - res files in the res folder
    - layout files in the layout folder
    - values files in the values folder
4. Add in dependencies: 
    - Add openGl dependency to Manifest folder: 
         <uses-feature android:glEsVersion="0x00020000" android:required="true" />
          -----(Place between /activity> and /aplication> like the example below)---------
         </activity>
        <receiver android:name=".Notification_receiver"/>
        <receiver android:name=".Notification_receiver2"/>
        <receiver android:name=".Notification_receiver3"/>
        <receiver android:name=".Notification_receiver4"/>
        <receiver android:name=".Notification_receiver5"/>
        <receiver android:name=".Notification_receiver6"/>
        <receiver android:name=".Notification_receiver7"/>
        <receiver android:name=".Notification_receiver8"/>
        <receiver android:name=".Notification_receiver9"/>
        <receiver android:name=".Notification_receiver10"/>
    </application>
    
    - Under 'Gradle Scripts' (Module: app), add in these dependencies in  dependencies {}:
          implementation "android.arch.persistence.room:runtime:1.1.1"
          annotationProcessor "android.arch.persistence.room:compiler:1.1.1"

Build -> Run 
